import {ServiceParams} from "@network/types/ServiceParams";
import {instance} from "./instance";

// TODO настроить класс, который генерирует Api сервисы

// const service = async <R, B, P>({method, data, url}: ServiceParams<B,P>): Promise<R> => {
    // return await instance.request<R, R>({
    //     url,
    //     method,
    //     params: {taskId: '1'},
    //     data
    // })
// }

// export const Network = {
//     service
// }